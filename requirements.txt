# ===== Core web =====
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
pydantic>=2.11.0
pydantic-settings>=2.6.0

# ===== DB (Postgres prod, SQLite dev/tests) =====
sqlalchemy[asyncio]>=2.0.40
asyncpg>=0.29.0
alembic>=1.14.0
aiosqlite>=0.20.0

# ===== PDF: единый стек reportlab + pypdf (см. ADR-002) =====
# НЕ добавлять pymupdf — выбран pypdf как единый стек с edo-stamps.
reportlab>=4.2.5            # генерация overlay-слоя (текст в координатах)
pypdf>=4.0.0                # открытие подложки ФНС + zero-loss merge_page
pdfplumber>=0.10.0          # извлечение текста+координат (pixel-diff координатный уровень)
pypdfium2>=4.0.0            # растеризация PDF→PNG (pixel-diff визуальный уровень)
Pillow>=10.0.0              # сравнение PNG

# ===== Excel / data =====
openpyxl>=3.1.5             # парсинг ОФД + извлечение координат из XLSX-шаблона
xlrd>=2.0.1                 # legacy xls

# ===== HTTP =====
httpx>=0.27.0               # async (DaData, заменяет requests из edo-stamps)

# ===== Observability =====
structlog>=24.4.0
python-json-logger>=2.0.7
