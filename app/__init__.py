"""
Compat-shim для legacy-импортов `from app.*` из модулей, которые
были скопированы из проекта `usn-declaration` (см. scripts/sync_sources.sh).

В оригинальном репозитории эти модули находились внутри пакета `app/`.
При миграции в EDO_COMPLETE они переехали в `modules/declaration_filler/`,
но часть импортов вида `from app.services.X import Y` осталась
в исходниках. Этот пакет-shim перенаправляет их на актуальные локации.

НЕ использовать этот пакет в новом коде. Все новые модули импортируют
напрямую из `modules/`.
"""
